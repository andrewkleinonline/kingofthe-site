<div class="response">
  <%= response.user.first_name_and_last_initial %>: <%= response.content %>
  <%= render partial: 'votes/button', locals: {response: response} if logged_in? && response.prompt.current %> -- <%= response.votes.count %> votes!
</div>
